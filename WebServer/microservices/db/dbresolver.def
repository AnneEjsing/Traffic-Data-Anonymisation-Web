BootStrap: docker
From: python:jessie

%files
    dbresolver.py /
    requirements.txt /

%post
    python3 --version
    python3 -m pip install --upgrade pip
    python3 -m pip install -r requirements.txt

%runscript
    python dbresolver.py